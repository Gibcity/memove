apiVersion: v1
kind: Service
metadata:
  name: {{ include "memove.fullname" . }}
  labels:
    app: {{ include "memove.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: {{ include "memove.name" . }}
