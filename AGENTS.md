# AGENTS.md — memove

> **Purpose:** Single bootstrap file for AI coding agents. Read this first on every new
> session. Stable, short, machine-checked. Human-facing docs live in `README.md`,
> `docs/wiki/`, and `docs/planning/` (planning briefs and contracts).

This file is read by Codex, Claude Code, Cursor, Gemini CLI, Zed, aider, and others.
Keep it under **~300 lines**. Anything longer belongs in a linked doc.

---

## 1. What this is

Self-hosted, real-time collaborative travel planner. NestJS + React 19 + Vite +
better-sqlite3 + MCP server. Single pnpm workspace, three packages, one shared
schema package. AGPL-3.0.

**Do not edit this file casually.** It's the first thing every cold-start agent
reads. Drift here multiplies across every future session.

---

## 2. Repo layout (memorize this)

```
memove/
├── apps/
│   ├── web/            # React 19 + Vite + Tailwind + Leaflet/Mapbox (PWA)
│   └── api/            # NestJS 11 + Express + better-sqlite3 + MCP + WebSocket
├── packages/
│   └── shared/         # @memove/shared — Zod schemas, single source of truth
├── docs/               # System-notices.md and other domain guides
├── wiki/               # Operator-facing docs (Unraid, photo providers, MFA…)
├── scripts/            # Build + dev + eval orchestration (run from repo root)
├── pnpm-workspace.yaml
├── package.json        # @memove/root — orchestration only; do NOT add deps here
├── pnpm-lock.yaml      # committed
├── Dockerfile          # multi-stage; build target is the workspace root
└── AGENTS.md           # ← you are here
```

**Workspaces:** `apps/web` (@memove/client), `apps/api` (@memove/server), `packages/shared` (@memove/shared). `@memove/shared` builds first; both `@memove/client` and `@memove/server` depend on it via `workspace:*`.

**Co-location rules:**
- `apps/web/src/repo/` — IndexedDB (Dexie) repos per domain (`*Repo.ts` files)
- `apps/api/src/nest/<domain>/` — NestJS module: `*.module.ts`, `*.service.ts`, `*.controller.ts`, `dto/`
- `packages/shared/src/<domain>/<domain>.schema.ts` — Zod schemas + inferred TS types

---

## 3. Setup commands

From `memove/` root:

```bash
pnpm install                            # workspace install
pnpm --filter @memove/shared build      # MANDATORY before client/server dev
pnpm dev                                # concurrently runs shared watch + server + client
```

Server listens on `:3000`, client on `:5173` (Vite default). SQLite lands at
`apps/api/data/<env>.db` (gitignored).

**One-liner for first-time setup:**
```bash
pnpm install && pnpm --filter @memove/shared build && pnpm dev
```

---

## 4. Quality gates (run from repo root, in this order)

```bash
pnpm --filter @memove/shared test       # 100% must pass; shared is the contract
pnpm --filter @memove/server typecheck  # tsc --noEmit, zero errors
pnpm --filter @memove/client typecheck  # tsc --noEmit, zero errors
pnpm --filter @memove/server lint:check # eslint, zero warnings
pnpm --filter @memove/client lint:check # eslint, zero warnings
pnpm --filter @memove/server test       # vitest unit + integration
pnpm --filter @memove/client test       # vitest + RTL
pnpm --filter @memove/client e2e        # playwright (needs server running)
pnpm build                              # all three workspaces
```

**Coverage floor:** 80% (enforced by CI). PRs that drop it are closed.

**Never run `pnpm build` mid-session** unless preparing a release — dist/
artifacts are gitignored but pollute the working tree and confuse IDE indexing.

---

## 5. Code style — the non-negotiables

1. **TypeScript strict mode** in every package. `noUncheckedIndexedAccess`,
   `exactOptionalPropertyTypes` are on.
2. **No `any`.** Use `unknown` + Zod parse, or define a type.
3. **No cross-package imports** between `apps/web/src/` and `apps/api/src/`. The
   seam is `@memove/shared` (Zod schemas) + the server's OpenAPI spec.
4. **Conventional commits** enforced by CI. Format: `type(scope): message`.
   Scopes mirror the domain (`feat(relocation):`, `fix(maps):`).
5. **Match existing style.** No reformatting unrelated lines. No "while I'm here" cleanups.
6. **Tests ship with the change.** New service → new test file. No exceptions.
7. **80%+ coverage** on the changed code path.

---

## 6. Domain conventions (the parts that bite)

### 6.1 Add-ons
New top-level features go through `isAddonEnabled(ADDON_IDS.X)`. The `relocation`
add-on is the canonical example — mirror its structure (controller, service,
schema, register in `addons.ts`).

### 6.2 Auth / guard order
Every controller uses `@UseGuards(JwtAuthGuard)` + `@UseInterceptors(IdempotencyInterceptor)`
on writes. Re-use `JwtAuthGuard`, do not roll your own. Passkey/WebAuthn handled
by existing endpoints — extend, don't duplicate.

### 6.3 Database
- `better-sqlite3` everywhere. Synchronous API by design.
- One module owns its tables (e.g. `trips/` owns `trips`, `days`, `packing_items`).
- Migrations live in `apps/api/src/<domain>/migrations/`, run in
  `database/runMigrations()` at boot. New table → new migration, no exceptions.

### 6.4 Realtime
WebSocket gateway is one per domain (`TripsGateway`, `CollabGateway`). Client
subscribes via `useWebSocket(domain)`. Don't open raw sockets from React.

### 6.5 Schema-first
**Define the contract in `packages/shared/src/<domain>/<domain>.schema.ts` before
writing server or client code.** Server validates with `.parse()`, client
imports the inferred type. OpenAPI is generated from the server controller
decorators — keep them in sync with the Zod schema.

### 6.6 Errors
Server throws typed exceptions from `@nestjs/common` (`NotFoundException`,
`BadRequestException`, etc.). No raw `throw new Error('something')`.
Client surfaces errors via the toast store — never `console.error` in production code.

---

## 7. Environment & secrets

- `apps/api/.env` — gitignored. Copy from `.env.example`. Required keys are
  documented there with inline comments.
- `apps/api/data/` and `apps/api/uploads/` — user data, gitignored.
- No secrets in client bundle. If you need a key client-side, scope it to
  public-provider keys (Mapbox, Google Places) and document it in `wiki/`.

---

## 8. Hot files agents touch repeatedly

These will be edited often; check them before assuming structure changed:

| Path | What lives here |
|---|---|
| `apps/api/src/addons.ts` | Add-on registry. New feature → register here. |
| `apps/api/src/nest/app.module.ts` | Module wiring. New module → import + add. |
| `packages/shared/src/index.ts` | Re-exports every domain schema. |
| `apps/web/src/main.tsx` | Router + providers. |
| `apps/web/src/components/SystemNotices/` | In-app notice renderers (modal/banner). |
| `apps/api/src/system-notices/registry.ts` | Server-side notice registry. |
| `pnpm-workspace.yaml` | Workspace packages. |

---

## 9. For subagents: how to return work

Every delegation must end with this confirmation block (see root
`CURRENT-WORK.md` for context):

```
### CONFIRMATION
- Commit: [hash or "not committed"]
- Files changed: [exact paths, one per line]
- TSC: [pass/fail + error count]
- Tests: [N pass / N fail or "not run"]
- Skipped: [what was skipped and why, or "none"]
- Push: [yes/no]
```

**Do not edit `CURRENT-WORK.md`** — only the orchestrator updates it.
**Do not push without `git status --short` showing a clean tree** other than
your intended changes.

---

## 10. Anti-patterns (will fail review)

- ❌ Importing `from '@memove/server'` in client code (or vice versa)
- ❌ `any`, `// @ts-ignore`, `as any`
- ❌ Direct `better-sqlite3` calls from controllers (go through the service)
- ❌ New dependency without a justification comment + wiki entry
- ❌ Touching files outside your task scope "while I'm here"
- ❌ Reformatting unrelated code
- ❌ Secrets in client bundle or committed `.env`
- ❌ Bypassing `IdempotencyInterceptor` on write endpoints

---

## 11. When this file is wrong

This file **will drift.** When it does:

1. Edit it. It's the cheapest fix.
2. If the change is bigger than ~30 lines, move the detail to `docs/wiki/` and
   link from here.
3. PR reviewers: block merges that leave this file inconsistent with reality.

The `validate-agents-md` check in CI verifies this file is <300 lines and
references real paths (see `scripts/check-agents-md.mjs`).

---

## 12. See also

- `PLAN.md` — execution plan: MapLibre migration, repo consolidation, quality debt backlog
- `README.md` — human-facing project overview, screenshots, feature tour
- `wiki/` — operator docs (Unraid install, photo providers, MFA, 2FA rotation)
- `docs/system-notices.md` — server-side notice system (the most-edited domain guide)
- `docs/planning/CONTRACT.md`, `docs/planning/INVENTORY.md`, `docs/planning/BRIEF-*.md` — planning docs for the
  relocation add-on (read these before relocation tasks)
- `CONTRIBUTING.md` — PR + commit + branch policy
- `SECURITY.md` — vulnerability reporting